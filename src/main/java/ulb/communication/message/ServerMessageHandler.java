package ulb.communication.message;

/**
 * Interface for classes that receive messages on the server side.
 */
public interface ServerMessageHandler {
	// public void handle(TeamCompositionMessage message);
	// public void handle(PickedActionMessage message);
}
